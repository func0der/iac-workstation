#!/bin/bash

SCRIPTS_BASE_PATH=~/Projects/scripts
SCRIPTS_GIT_BASE_PATH=$SCRIPTS_BASE_PATH/git

# set PATH so it includes user's private bin if it exists
if [ -d "$HOME/.local/bin" ] ; then
    PATH="$PATH:$HOME/.local/bin"
fi
